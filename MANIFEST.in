# the whole test suite, including its stub server, so a source distribution
# can actually run the tests rather than shipping half of them
recursive-include tests *.py

# example profile and the guide to capturing real ones
recursive-include profiles *.json *.md
